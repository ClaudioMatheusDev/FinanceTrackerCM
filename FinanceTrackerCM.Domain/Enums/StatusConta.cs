namespace FinanceTrackerCM.Domain.Enums{
    public enum StatusConta
    {
        Ativa, // A conta está ativa e pode ser usada para transações
        Inativa // A conta está inativa e não pode ser usada para transações
    }
}